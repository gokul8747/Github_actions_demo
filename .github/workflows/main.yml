name: hello-world
'on': push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Acess repo
        uses: actions/checkout@v2
      - name: my-step
        run: echo "Hello World!" >> demo.txt
